/** Keep in sync with `.piece-float` transition and `piece-hop` duration in styles.css. */
export const PIECE_STEP_MS = 200;

/** Full-screen match start countdown (admin starts match). */
export const MATCH_START_COUNTDOWN_FROM = 5;
export const MATCH_START_COUNTDOWN_TICK_MS = 1000;

/** Marriage deal animation: one card at a time around the table. */
export const MARRIAGE_DEAL_CARD_MS = 100;
